# Rail Components

Portal drawing object-workbench rail components belong here.

`ObjectWorkbenchRail` is the canonical hidden workbench rail. `ConfiguratorRail` remains here for the estimates tab compatibility surface.
