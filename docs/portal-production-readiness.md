# Portal Production Readiness

Status: Active evolving tracker.

Last updated: 2026-08-06.

Purpose: keep agents and maintainers aligned on the path to a first-class, production-grade internal portal. This doc is the dashboard for current readiness, blockers, priorities, parallel lanes, and next actions. Detailed behavior rules stay in the canonical docs linked below.

## End Goal

The portal should be a reliable internal production system for day-to-day Sanctuary staff work.

Production grade means:

- Quality gates are green, repeatable, and enforced in CI.
- Staff, admin, and public-token access boundaries are explicit and tested.
- Supabase migrations, RLS, grants, RPCs, and server clients match the documented source-of-truth boundaries.
- Core workflows are safe under real use: contacts, projects, estimates, quotes, invoices, job packs, schedule, design workbench, Design List, and Running Jobs.
- Server-owned side effects such as email, PDFs, public tokens, invoice creation, and job-pack generation are tested at the domain boundary, not only in UI render tests.
- Long-running server work has a logged durable queue, private frozen inputs, lease-fenced workers, effect checkpoints, safe status history, and an evidence-backed rollout path.
- Heavy portal surfaces load and respond within their budgets.
- Local-first flows show pending, failed, retry, conflict, and locked states clearly.
- Compatibility and legacy fallback paths are named, visible, tested, and removed when no longer needed.
- Large files and high-risk surfaces have clear ownership, small modules, and focused tests.
- Large portal files follow `docs/file-decomposition-and-ownership.md` before major feature expansion so the portal remains SaaS-extractable.
- Unused code, stale exports, and old dependencies follow `docs/code-retirement-and-bloat-control.md` so cleanup is systematic and proof-driven.
- Docs match the implementation and are updated as part of the work.

## How To Use This Doc

Agents must use this doc as a living checklist, not a static plan.

Before taking non-trivial portal production-readiness work:

1. Read `../AGENTS.md`.
2. Read `docs/agent-playbook.md`.
3. Read `docs/change-routing.md`.
4. Read `docs/maintainability-principles.md` for bugfixes, migrations, interaction wiring, or hotspot work.
5. Read this doc.
6. Read the smallest relevant canonical doc for the area being changed.
7. Scan `docs/decision-log.md` for matching risks or past mistakes.
8. Check `git status --short` and leave unrelated changes untouched.

When work changes readiness status, blockers, priorities, verification commands, parallel lanes, or newly discovered risks, update this doc in the same pass. Prefer updating existing rows and checklist items over adding long new sections.

Use `npm run docs:readiness` for an advisory summary of tracker age, at-risk rows, and unchecked checklist counts. The report warns when this tracker is more than 7 days old, but it does not replace rerunning the listed commands or manual checks.

Do not duplicate detailed rules from canonical docs here. Link to them, then keep this doc focused on current readiness state, next actions, and coordination.

## Current Readiness Snapshot

This snapshot records the most recent known production-readiness state from the portal review and follow-up checks; unrelated lanes below retain their own dated refs and evidence. The Project Work portfolio migration and application release are live: all 1,151 production projects are in V2 without deleting legacy audit rows, production database/application postflight is clean, and the authenticated GET-only production browser proof passes. The separate commercial-trust lane retains local, executable-database, rendered-artifact, and staging quote-smoke evidence only; its real provider/public-token journeys and production migration remain separately controlled.

Status describes the last verified signal, not an implicit current-head result. `Green` means the recorded evidence passed at its stated date or run; `Yellow` means the area is incomplete or its evidence no longer covers the current ref; `Red` means a current blocker is known. Any row without an explicit current ref remains last-known evidence and must be re-run before a release decision.

| Area | Status | Last Known Signal | Next Action |
| ----------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Portal tests | Green | On 2026-07-28 the local commercial-trust worktree passed 403 portal Vitest files with 2,156 tests plus 11 intentional skips, the full 53-file/286-test marketing suite, focused commercial/route/fixture contracts, repository typecheck and lint. The executable PGlite contract now covers both commercial migrations, and the provider-free authenticated staging quote smoke passes. | Keep the broad repository gates blocking and require separately approved controlled-recipient staging side-effect journeys before release. |
| Lint and guards | Green | `npm run portal:doctor:quick` completed `npm run lint`, including docs guard, cache guard, brand guard, mojibake, and ESLint. | Keep lint in quick doctor and portal PR CI. |
| Portal route bundle budgets | Green | The reusable analyser detects Turbopack lazy groups honestly and now reports the authenticated exact-frame catalogue separately from each route runtime. On 2026-08-06, the complete data-free catalogue measured 562.5/117.5 KiB raw/gzip at its largest route-specific manifest view; its cap is that fresh measurement plus 5%. Project and workbench ceilings were likewise rebased from the same production build while heavy 3D runtime remains intent-loaded and separately capped. | Keep the separate post-auth shell, route runtime, and specialist lazy ceilings blocking; rerun the fresh production build and aggregate assertion after shared-shell, Calculator, or workbench import-boundary changes. |
| Current portal UI baseline | Green | The checked-in portal UI, shared catalogue, responsive shell, status contracts, focus-managed modal/drawer, current production patterns, reduced-motion and contrast rules, focused component coverage, and Projects Index/Detail browser proof are implemented. Full portal tests, an isolated fresh production build, the unchanged Project Detail ceiling, and the dedicated 1440/1280/1024/768/390 plus 200% zoom production Playwright re-audit are green. The separate bundle-budget row records current route evidence. | Treat the current rendered portal as canonical. Re-run the relevant route evidence when an explicitly approved UI change touches that surface; there is no pending portal-wide Foundation rollout. |
| Portal performance evidence | Green | On 2026-08-08 the final optimized production audit passed all six authenticated journeys, including a real two-minute idle Dashboard revisit. Warm feedback/useful/live-data was Projects 58/97/1,971 ms, Work Queue 57/74/1,955 ms, Calculator 90/102/1,974 ms, Commercial Quotes 72/85/967 ms, Commercial Invoices 63/71/940 ms, and idle Dashboard 18/45/2,906 ms; the idle Dashboard core API itself completed in 1,251.5 ms and the separate queue in 1,478.4 ms. The earlier exactly-five-run shell aggregates and cold Project Detail guard remain recorded in `docs/testing-and-qa.md`. | Keep the 100/500 ms shell ceilings plus Projects 3,500 ms and Work Queue 2,500 ms live-data p75 ceilings enforced. Retain separate feedback, useful-content, explicit live-data, and generic settlement markers so incomplete data is never relabelled as fast. |
| Portal data locality | Yellow | A live production `/login` response entered Vercel through `syd1` but `x-vercel-id` recorded the function execution hop at the default Washington `iad1` region. The repository has no Portal region override. Local Supabase requests reached the Melbourne Cloudflare edge, but neither local configuration nor available dashboard access proved the database's primary region. | Confirm the exact Supabase primary region in its dashboard. If it is Oceania/Sydney, add a Portal-owned `syd1` Vercel function-region setting as a separate deployment slice, then rerun the same authenticated production suite before and after. Do not move functions based only on the API edge location. |
| Offline application shell | Yellow | Current code gives normal staff/admin routes exact data-free destination frames, preloads the small core frame chunks after live role verification, and installs a release-versioned static-only service worker. The already-open app can navigate registered frames offline without retaining the previous route's protected DOM. Focused transition, cache, auth, and owner-cleanup contracts cover the boundary; HTML/RSC/API caching and offline hard refresh/new-tab startup are deliberately absent. | Run the optimized build, route bundle budgets, authenticated route-specific performance suite including the idle Dashboard revisit, and a production-mode soft-offline browser journey before promoting this row. Keep hard-reload offline work as a separate security-reviewed slice. |
| Portal Web Vitals | Green | Identifier-free first-party ingestion, admin p75/p95 summaries, route sanitisation tests, RLS/grants, and the 30-day `pg_cron` migration are implemented. On staging, the named daily cron job was present, the cleanup removed a 31-day fixture, retained a recent fixture, and the remaining fixture was then removed. | Promote the verified forward migration through the normal production deployment path and keep the 30-day job monitored. |
| Production security audit | Green | On 2026-07-28 both applications resolve the root PostCSS override at 8.5.18 and `npm audit --omit=dev` reports zero vulnerabilities on Next.js 16.2.11. | Keep `npm run audit:security` blocking and do not weaken the override without a fresh audit. |
| Repository key incident | Red | Commit `db20ed2e` removed tracked private-key material discovered by the repository security test. The material remains in Git history and must be treated as compromised; no rotation/revocation evidence is recorded here. | Rotate or revoke the owning credential and audit downstream use. Do not rewrite Git history. A clean current-tree scan is not incident closure. |
| Security and data boundaries | Yellow | The 2026-07-28 commercial-trust worktree passes the audit, service-role inventory, browser-Supabase inventory, static and executable migration contracts, route tests, public-token tests, deterministic provider-adapter tests, and provider-free staging quote smoke. `20260728_000001_commercial_workflow_trust.sql` plus the stale-conflict correction are applied only to the positively identified staging project; production was not touched. | Complete separately approved controlled-recipient send/resend/public-acceptance/invoice-delivery QA, then use the normal reviewed production migration path. |
| Portal build | Green | On 2026-07-28 an isolated Next.js 16.2.11 portal build compiled, completed TypeScript, and generated 77 pages while the existing port-3001 dev server remained untouched. The marketing build generated 65 pages. | Keep the build preflight and isolated-output technique; rerun after quote, invoice, PDF, fixture, or Next config changes. |
| Typecheck | Green | `npm run typecheck` passed on 2026-07-28 across marketing, portal, the standalone Node worker, `@sp/costing`, `@sp/email-provider`, `@sp/geometry`, `@sp/jobs`, `@sp/quote-format`, and `@sp/theme`. | Keep typecheck in quick doctor and CI, including the dedicated Background Jobs workflow for changes to the durable-job foundation. |
| Browser smoke | Green | Portal Quality passed fixture browser/performance smoke and authenticated routing smoke on staging on 2026-07-19. The authenticated calculator trust suite also passed all 10 checks against its dedicated revisioned three-module/two-pergola V2 scenario at 1600px, 1366px, 1024px, and 768px; the 5-test infill lane covered exact piece/purchase/CSV checks, production-first tablet ordering, unmanufacturable stock routing, and invalid partial-edge rafter routing using browser-draft-only mutations. | Keep the canonical calculator, authenticated smoke, and authenticated performance gates on compatible provisioned scenarios. |
| Project command centre | Green | Overview V2 composes journey, detailed stage, server-owned state/freshness, one V2 Project Work surface, exact current design/commercial truth, and bounded recent notes/events through extracted owners. Duplicate command/task UI and legacy project-task presentation/controllers are retired. Focused tests, the real-composition fixture, unchanged bundle budgets, isolated authenticated staging proof, production deployment/postflight, and authenticated GET-only production Projects/Dashboard/Work Queue/real Overview proof pass. | Monitor normal staff use and keep the guarded authenticated production smoke available without retaining a standing production QA login. |
| Project work items V2 | Green | The exact portfolio rollout was rollback-rehearsed, applied, and postflight-verified in staging and production. Production has 1,151/1,151 model markers and operational states, one independent cohort ledger row, 1,151 rollout events, 1,109 Active and 42 Closed projects, and no active Call/Site Visit/legacy-review work. The four legacy tables remain at exact preflight counts as audit/rollback evidence, while their product readers/writers are retired. | Monitor queue cardinality and repair signals during normal staff use. Keep future project creation on the canonical V2 command and preserve the production read-only smoke as a dispatchable release check. |
| Docs and routing | Yellow | On 2026-07-28 docs guard, mojibake, and docs-impact checks are clean for the commercial-trust worktree, and the estimate, quote/invoice, local-first, email/audit, auth, architecture, schema, testing, security, readiness, and decision-log owners are aligned. The broader navigation report still contains historical density findings. | Keep the canonical owner docs aligned and treat unrelated navigation-density work as a separate slice. |
| File decomposition | Yellow | `CalculatorGridClient.tsx` is now below the critical threshold at roughly 1.1k lines and owns final configuration/workspace composition only. Calculator estimate/project loading and duplicate-draft orchestration, input normalization, validation construction, result-derived presentation, save readiness, result freshness, command bar, project picker, outer workspace shell, preview/configuration presentation, browser-draft persistence/session state, module navigation, house-footprint gesture/lifecycle control, job/module input mutation policy, blinds/flashings mutation state, configuration-section presentation, structure/site/workflow field-schema generation, issue model/focus navigation, infill lifecycle/actions/interactions/cost coordination/presentation, save orchestration/dialogs, blinds/flashings editor markup, materials-debug behavior, preview/pricing presentation, preview splitting, price-impact presentation, and quote-status helpers now live outside the client. It no longer owns draft hydration, project/estimate fetch coordination, revision duplication, local persistence effects, module action policy, house-footprint drag state, job/module field compatibility policy, blinds input drafts, flashing row mutations/primary-length linkage, result-module selection, view status, impact baselines, derived-output summaries, preview sorting, section ordering/layout, field option/schema construction, validation policy, issue model/focus timing, infill domain/UI lifecycles, outer workspace rendering, save dialog presentation, blinds/flashings rendering, materials-debug effects, preview-detail cards, preview-resize effects, pricing-summary markup, or price-impact styles. | Keep the client composition-only; extract configuration-schema or workspace-prop assembly before future inline growth, and run `npm run files:changed` before handoff. |
| Code retirement | Yellow | `docs/code-retirement-and-bloat-control.md`, `npm run dead-code:report`, `npm run dead-code:changed`, and `npm run dead-code:changed:strict` make unused files, exports, types, and dependencies visible while blocking only newly added unused files locally. Worktree strict mode blocks undeclared dirty-tree lanes during explicit strict verification. Portal Quality keeps dead-code and aggregate architecture strict reporting advisory, while the separate `files:changed:strict` decomposition step is a blocking pull-request gate. | Calibrate the dead-code registry, delete proven candidates in small PRs, and keep the separate blocking decomposition registry current for touched critical files. |
| Local-first flows | Yellow | React Query server responses are memory-only and mount only after live role verification; durable browser storage is reserved for per-user drafts and queued local-first work. Logout, owner change, and access loss clear that owner's local-first/session/theme data plus retired query-cache keys, while runtime start/stop owns listeners, timers, and subscribers. Tests cover owner isolation, confirmed discard, partial cleanup failure, auth races, and stale-handler shutdown. | Retain authenticated manual pending/retry/conflict QA and verify confirmed-discard sign-out on a shared staging device before release. |
| Quote/invoice/job-pack side effects | Green | The 2026-07-28 Option B worktree adds exact estimate/quote intents, monotonic quote revisions, one current draft, atomic delivery preparation, private frozen email intents, provider checkpoints, replay-safe finalisation, atomic acceptance/invoice identity, truthful recovery UI, deterministic retry/attention fixtures, and rendered quote-PDF pagination coverage. Disposable PostgreSQL caught an acceptance/invoice ambiguity before staging; staging smoke then caught SQLSTATE `40001` retrying a stale write into a delayed `500` plus Next's server bundle rewriting a module-relative font to an unreadable browser URL. All three are corrected, both migrations pass rollback/apply/replay, and the authenticated staging read/update/stale/recovery/artifact/render path is green without provider dispatch. | Perform deliberate staging send/resend/public acceptance/invoice-delivery checks with controlled recipients only after separate approval; production deployment remains pending. |
| Durable background jobs | Yellow | JOB-01 owns the durable ledger/private payload/logged queue/effect history; JOB-02 adds the dark RPC-only worker. JOB-03 repository work adds `@sp/email-provider`, exact job/effect provider identity and bytes, a database-frozen 20-hour same-key retry window inside the documented 24-hour provider retention, durable email checkpoints, the required ten hard-crash points plus one business-finaliser lost-return boundary, bounded signed webhook verification, minimal append-only receipts, and a seventh reconciliation migration with signed-webhook reconciliation plus lease-fenced local acceptance/message-ID quarantine. Local JOB-03 verification passes 47 provider tests, 38 application integration tests, 134 worker tests, and 144 combined job/security/static-migration tests, plus typecheck, lint, security, and all three production builds. Background Jobs [run 29723041212](https://github.com/velt-design/sanctuary/actions/runs/29723041212) passes the seven-migration matrix on PostgreSQL 18/PGMQ 1.10.0 and Supabase PostgreSQL 17/PGMQ 1.5.1, plus contracts/integrations and the worker artifact/container gates. Deposit-invoice, quote, and automation ownership remain request-bound, and the worker still has no registered commercial handler or producer. | JOB-03 is Green; the lane remains Yellow because JOB-04 through JOB-08, shared-environment review, and controlled rollout are pending. No shared database migration, production deployment, real email, enabled handler/producer, or rollout is implied. |
| Schedule workflow | Green | `npm run test:portal:schedule` passes 55 files and 420 tests, covering Schedule V2 APIs/readiness, atomic Gantt adjustment, strict affected-job preview/re-preview/confirmation, cross-instance save ownership, malformed-response rejection, optimistic rollback/reconciliation and cache authority, fresh-release Board targeting, exhaustive insertion positions and cross-crew order, rapid same-lane queuing, temporary-to-server identity rebase, disjoint-crew concurrency, out-of-order response replay, blocked uncommittable gestures, grouped actions, silent normal Board persistence, resource-scoped recovery, Board/Gantt keyboard/responsive behavior, the retained Site Visits capability, command boundaries, and legacy fallback isolation. Board and Gantt share one presentation owner for forecast workload, commitment, drift, client-update and schedule-issue language. Board treats the placed card as normal feedback and keeps placement gestures available while commands persist: commit-ambiguous outcomes retain that placement through bounded authoritative reads, apply at most one correction, and pause only the affected project/lane when staff action is required; Gantt retains its route status. The synthetic confidence gate covers six operational widths and 200% zoom, including held-pointer beginning/middle/end and cross-crew drops, silent slow-persistence placement, actionable failure/stale states, a purposeful small-screen Gantt agenda, focus return and zero staff writes. Authenticated read-only renders confirm both Board and Gantt against existing records at desktop and mobile width without changing Schedule data. Network/408/unexpected 5xx ambiguity reconciles authoritatively; HTTP 501 is the explicit pre-commit schema/RPC-unavailable exception. Site Visits remains hidden from normal navigation. | Keep live readiness and manual Board/Gantt mutation persistence checks in release QA with staff credentials and a migrated database; QA Site Visits only through direct compatibility access or an approved reactivation. Non-placement Board actions retain their guarded lifecycle while placements are pending. Add a guarded RPC or per-crew revision before claiming protection from near-simultaneous staff edits. |
| Design workbench | Green | Focused object-first helper coverage passed on 2026-05-21; the current state-lane anchors include `apps/portal/lib/drawings/state/objectFirstWorkbenchFixtures.test.ts`, `apps/portal/lib/drawings/state/objectWorkbenchStatusModelRoofValidation.test.ts`, and `apps/portal/lib/drawings/state/projectPergolaViewerScene.test.ts`. The broader last-known signal remains `npm run test:portal:workbench` on 2026-05-04 with 58 Vitest files and 589 tests, followed by the no-auth browser fixture pass. | Keep manual edit/save/reload, pricing-source rollout, and high-risk visual QA in release checks with authenticated staff data; rerun the full workbench and browser gates before treating this as release-complete. |
| Geometry/costing migration | Yellow | The workbench has a solved geometry artifact, a package-owned `GeometryQuantityTakeoff` derived from `Assembly3D`, and shadow `workbench_solved` commercial payload coverage that maps that takeoff. Baked fixture parity and representative saved estimate snapshot parity now compare `calculator_compat` and `workbench_solved`, with drift classified as authored intent, solved geometry, physical takeoff, or commercial mapping; live estimate and quote pricing still use the calculator costing path. | Keep the commercial path shadow-only, extend geometry takeoff buckets as solver support grows, and retire compatibility only after manual workbench QA and parity are stable. |
| Calculator Brain configuration | Yellow | Repository implementation adds package-owned typed configuration/validation/diff/impact, admin history/draft/clone/publish UI and APIs beneath Pricebook, immutable database versions with atomic audited publication, fail-closed staff reads, exact calculation provenance, and estimate version linkage. Focused costing/estimate/admin/control-centre coverage passed 31 files and 301 tests; the full portal Vitest suite, typecheck, lint/docs/package guards, and static migration contract are green. `20260723_000001_costing_configuration_versions.sql` is applied and verified. On 2026-08-04 an explicitly approved authenticated production journey reset the stale first draft to the complete active `v1.8` snapshot, saved and validated it, proved `0` changed values and `0.0%` movement in all four representative scenarios, and atomically published **Version 1 — Current portal baseline**. The live staff calculator and public calculator now use the publication; the public API returns customer-safe results with `versionNumber: 1`. | Keep Yellow until authenticated journeys finish invalid-field and stale-conflict feedback, clone/rollback, and a post-publication saved estimate proves both frozen response provenance and `costing_config_version_id`. Do not send a real enquiry email merely to test autoresponder provenance; use controlled-recipient QA when separately approved. |

## Wave 1 Instant Portal Status

This table maps the original Instant Portal plan to the integrated Wave 0 and Wave 1 stack. Green means the stated outcome has code, focused tests, and current performance or bundle evidence; Yellow names the exact remaining work.

| Item | Status | Evidence and remaining work |
| ---------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| NAV-01 | Yellow | The current implementation extends exact, data-free destination frames across normal staff/admin routes, keeps the persistent shell mounted, preloads only the small core frame chunks after live verification, and supports registered soft-offline navigation while the app remains open. Route-specific final-frame, continuity, idle-revisit, transition, and static-cache contracts replace heading/skeleton acceptance. The optimized build, bundle gate, authenticated performance run, and production-mode soft-offline browser proof remain required for this combined ref. |
| AUTH-01 | Green | Staff/admin helpers are explicit and nested server renders share one request-scoped access lookup: one verified `auth.getUser()` call plus one `portal_users` role read. There is no process/global private cache, and each API request still re-verifies through an auth-bound RLS client. A measured claims/JWKS experiment worsened fresh-server cold routes and was rejected rather than weakening the boundary. |
| READ-01 | Green | Project and contact lists have canonical staff API/query owners, auth-bound server clients, paginated loaders, concurrent independent reads, diagnostics, and browser-Supabase boundary coverage. |
| READ-02 | Green | Dashboard, Projects, and Contacts render their useful frames from the authenticated user's QueryClient first, then refresh through owned authenticated APIs with truthful pending/cached/fresh/failure states. None of the three page routes awaits its database index before rendering. |
| PROJECT-01 | Green | The 2026-08-06 current-ref production revalidation recorded Projects-to-project at 14 ms feedback and 19 ms useful-content p75 after the project shell began reading the current combined Projects-index cache directly. A cold direct link still promotes the small authenticated project/contact summary while the complete snapshot continues in the background. Exactly five runs measured cold useful content at 2,651/2,663/2,682 ms p50/p75/p95, below the fixed 2,699 ms guard; every project run had no overlay or long task. |
| PROJECT-02 | Green | Overview, Calculator, Commercial, and conditional Job Packs are isolated code boundaries with exact intent preloading and truthful local/snapshot-dependent states. Overview remains internally keyed as `activity`; Calculator keeps `estimates`; Commercial groups `quotes` and `invoices`; Job Packs keeps `job-packs`. Overview owns an independent command-centre query and can render during project-summary promotion while snapshot-owned notes/tasks remain explicitly updating. Project details and stage correction live in Overview at every width; the former Details rail/tab and Emails UI are retired. |
| BUNDLE-01 | Green | Fresh isolated Stage 1 build assertions pass all six route gates. Project Detail is about 2,434 KiB raw / 562.0 KiB gzip combined after the Overview command-centre boundary, below the preserved 3,014,656 / 757,760 byte cap. Three/React Three Fiber remains isolated behind exact `3D Review` intent and counted by Design Workbench, whose split allowance does not increase its previous 2,681,856 / 671,744 cap. |
| CALC-01 | Green | `useCalculatorCostingRequest` owns debounce, abort, newest-result protection, last-valid continuity, and errors without moving costing authority into the browser. The recorded five-run feedback is 40/47/58 ms p50/p75/p95, fresh result is 924/939/942 ms, and no overlay or long task was observed. Focused calculator coverage passed. This is last-known evidence, not a run at `005be1a7`. |
| WB-01 | Green | Workbench clients memoize one solved base by draft/project identity; selection, visibility, and viewport changes reuse that solved-model object while draft changes still solve. Focused coverage proves identity reuse. Fixture evidence remains request-free and overlay-free; selection measured 86/119 ms feedback/useful and Plan-to-3D 117/122 ms on the latest single run, both within regression budgets, with no long task. |

## Project Pages Speed Priority

Project pages are the highest product-performance priority. The Wave 1 results above remain historical evidence for the integrated stack that produced them; they do not automatically prove a later change to the current project shell, navigation, Calculator/Commercial composition, or shared UI owners. Revalidate the current checked-in UI in a production build before choosing a new optimization, then fix the slowest measured project journey at its owning read, cache, preload, bundle, or render boundary.

The product contract remains 100 ms visible feedback and 500 ms useful content for warm project navigation and project-tab intent. Fresh server settlement is measured separately and must not replace useful known content with a blocking overlay. Cold direct Project Detail keeps its existing 2,699 ms comparison guard until five current-UI production runs justify a tighter ratchet.

| Item | Status | Evidence and remaining work |
| --------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PPS-01 | Green | The 2026-08-06 local optimized production build completed exactly five authenticated performance repetitions against one integrated state. It recorded Projects Index, Projects-to-project, browser Back, cold Project Detail, and current project-tab feedback, useful-content, and background-settlement evidence. Preserve this as last-known evidence and re-run after a performance-relevant approved change; do not rebaseline from development compilation or a dirty mixed lane. |
| PPS-02 | Green | Current Projects Index and routine project opening pass the locked 100/500 ms gate. The pre-change current-ref Projects-to-project useful p75 was 1,329 ms; reading the freshest matching combined Projects-index response at the project-shell boundary reduced exactly-five-run feedback/useful p75 to 14/19 ms. Sidebar route prefetch is no longer viewport-wide and starts from hover, focus, touch, or pointer-down. The current-user QueryClient, immediate canonical URL feedback, truthful updating frame, quiet authoritative snapshot refresh, access-ending data hiding, and reversible browser Back remain intact. |
| PPS-03 | Green | The current production journey covers Overview, Calculator, Commercial Quotes/Invoices, and conditional Job Packs. The project frame now renders selected-tab state and the matching owned shell optimistically while `router.replace()` and specialist data settle in the background; Commercial applies the same rule to Quotes/Invoices. Exactly five authenticated production repetitions moved p75 feedback/useful shell from Calculator 151/154 ms, Quotes 141/512 ms, Invoices 147/149 ms, Overview 149/152 ms, and Job Packs 923/927 ms to 36/40 ms, 38/41 ms, 38/41 ms, 39/44 ms, and 37/41 ms respectively. All five current tabs pass the locked 100/500 ms gate, including the stronger sub-100 ms useful-shell objective. |
| PPS-04 | Yellow | Measure project and contact creation only after index, opening, Back, and tab journeys are green. Prefer immediate truthful submitted/pending feedback and an idempotent staff-API creation contract before adding persisted provisional entities. Add canonical client-generated IDs or local-first aliases only if measured latency or an explicit offline requirement justifies their reload, retry, discard, and dependent-action complexity. |

Execution order:

1. Keep PPS-01 as the last-known integrated baseline and re-run it after a performance-relevant approved change.
2. Keep PPS-02 green against the existing 100/500 ms ceiling; optimize only a measured regression or bottleneck.
3. Keep PPS-03 green against the current project tab registry and bundle boundaries.
4. Complete PPS-04 only after the everyday project journeys are green.
5. Keep JOB-04 through JOB-08 in a separate backend worktree while this lane is active; they may not edit project-page UI, shared shell, or current quote/invoice/job-pack presentation until the project-speed checkpoint lands.

## Wave 2 Instant Write Status

Wave 2 applies the same truthful-feedback rule to routine writes. Reversible staff changes may update the authenticated user's query/local-first state immediately and reconcile through the owning API in the background. Validation, permission, conflict, and server failures must remain visible and restore only the affected optimistic state. Hard delete, money state, email delivery, public-token actions, and other irreversible/customer-facing side effects remain server-confirmed.

| Item | Status | Evidence and remaining work |
| ----------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W2-MEASURE-01 | Green | The data-free `/qa/projects-index-mutation-fixture` mounts the production Project Index, Project Details, Contact Details, and manual-task mutation owners and holds persistence open in Chromium. Current full local evidence records 30/803 ms for index feedback/background completion, 5/819 ms for Project Details Done, 31/815 ms for Contact Details Done, and 36/803 ms for a task toggle. Visible feedback is timestamped inside Chromium when the real DOM state changes, excluding Playwright-driver round-trip noise. Each measured journey used one request with zero long tasks and no blocking overlay. Paired rejection journeys prove precise rollback plus visible Review/Retry state. Portal Quality uploads this schema-v2 artifact beside the workbench artifact. |
| W2-PROJECT-INDEX-01 | Green | Name, phone, address, explicit reviewed stage corrections, archive, and restore patch the current user's caches immediately, retain a small syncing marker, reconcile in the background, and roll back the affected value/scope on failure. Stage correction names the target stage and Project Work recalculation before submission. Active/archived/all membership and known counts move together. Hard delete remains server-confirmed. |
| W2-PROJECT-DETAIL-01 | Green | Done exits edit mode immediately and the desired values remain visible while the user-owned local-first queue persists them in order. Working copies survive route changes, reloads, and offline periods; transient failures retain optimistic data and retry, terminal validation/access/conflict responses roll back known data but retain the rejected draft for Review/Retry, and access-ending responses also refresh the protected read boundary. Matching working copies clear only when no newer edit or queued save exists. Fixture Chromium measured 5 ms visual feedback against an intentionally delayed 819 ms background completion. |
| W2-CONTACT-DETAIL-01 | Green | Done exits edit mode immediately and Contact Detail persists full normalized drafts through the authenticated user's ordered local-first queue. Working copies survive navigation/reload/offline; transient failures retain desired data and retry, while terminal failures restore confirmed caches and keep the rejected draft reviewable/retryable. Fixture Chromium measured 31 ms visual feedback against an intentionally delayed 815 ms background completion, with no overlay or long task; the paired 403 journey proved rollback plus retained review state. |
| W2-CREATE-01 | Yellow | Contact and project creation still wait for durable server IDs before navigation. Define provisional-ID/alias and reload behavior before making either create path local-first; do not fake a durable detail URL. |
| W2-TASK-01 | Green | Superseded: the unused legacy project-task mutation surface, fixture, route, and writers are retired. Current Project Work commands retain server-owned pending, committed, ambiguous-retry, row-version, and idempotency semantics; no legacy optimistic task toggle remains to measure. |
| W2-SIDE-EFFECT-BOUNDARY | Green | Hard delete, sends, accepts/declines, invoice state, public tokens, job packs, and Schedule RPC commands remain outside optimistic success. They may expose immediate pending feedback but must not claim completion before the server confirms it. |

## Wave 3 Durable Background Jobs Status

Wave 3 must keep queue durability, private input, worker fencing, external-effect idempotency, business finalisation, safe staff visibility, and rollout ownership separate. A registry entry or migration file is not a live worker.

| Item | Status | Evidence and remaining work |
| --------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| JOB-01 | Green | Repository implementation is complete: `@sp/jobs` owns kinds and transition policy; five forward migrations own the logged PGMQ queue, ledger, immutable private payloads, state/effect history, frozen per-job effect policy, random lease tokens, service-role RPC lifecycle, exact queue repair, provider-window recovery, explicit safe inspection, and a Supabase-specific explicit RPC allowlist. The local 4-file/71-test suite, typecheck, lint, architecture sweep, and production dependency audit pass. Background Jobs [run 29710584022](https://github.com/velt-design/sanctuary/actions/runs/29710584022) passed contracts plus the rollback-wrapped executable suite on PostgreSQL 18/PGMQ 1.10.0 and Supabase PostgreSQL 17/PGMQ 1.5.1. The harness intentionally covers minimal test prerequisites plus the five JOB-01 migrations, not the non-bootstrappable historical chain. Shared-environment deployment and review remain external gates. |
| JOB-02 | Green | Repository implementation and local gates pass: `apps/worker` is a standalone Node 22 runtime, dark by default and fail-closed with no commercial handlers; migration `20260720_000006_background_job_worker_runtime.sql` adds lease-fenced runtime context plus safe aggregate job/worker projections; `npm run test:jobs` passes 5 files/86 tests and `npm run test:worker` passes 9 files/76 tests. Background Jobs [run 29713940507](https://github.com/velt-design/sanctuary/actions/runs/29713940507) passes both real-PGMQ database legs and the non-root worker-container build. No shared database, deployment, producer, handler, or rollout is implied. |
| JOB-03 | Green | Repository implementation and local checkpoint gates pass: `@sp/email-provider` owns exact Resend bytes/key/hash/tags, explicit 24-hour provider-retention and 20-hour retry configuration, typed outcomes, timeout/abort, and raw-body Svix verification; the portal rejects oversized webhook streams before verification; the worker owns prepared/dispatch-started/accepted/finalised/uncertain coordination plus the required ten-point hard-crash matrix and one business-finaliser lost-return boundary. Migration `20260720_000007_background_job_provider_reconciliation.sql` owns append-only minimal receipts, signed-webhook reconciliation, and a lease-fenced local provider-acceptance RPC that atomically quarantines conflicting or cross-job-colliding provider message IDs. Background Jobs [run 29723041212](https://github.com/velt-design/sanctuary/actions/runs/29723041212) passes the seven-migration real-PGMQ matrix, contracts/integrations, and worker artifact/container gates. Deposit-invoice, quote, and automation/outbox owners remain authoritative. No shared database, production deployment, real email, handler, producer, or rollout is included. |
| JOB-04 through JOB-08 | Yellow | Pending. Workflow producer/handler migration, staff/admin job experience, and controlled rollout have no completion evidence yet. Existing quote, invoice, job-pack, automation, and email owners remain authoritative until their checkpoint lands. |

## Production-Grade Checklist

### Quality Gates

- [x] `npm run portal:doctor:quick` passes for routine local readiness.
- [ ] `npm run portal:doctor` passes for broad pre-merge readiness when Playwright auth/env and audit expectations are ready.
- [x] `npm run typecheck` passes.
- [x] `npm run docs:guard` passes.
- [x] `npm run text:mojibake` passes.
- [x] `npm run lint` passes.
- [x] `npm run test:portal` passes.
- [x] `npm run build:portal` passes.
- [x] `npm run portal:bundle-budget` passes every unchanged route ceiling; the isolated 2026-07-30 route-optimization build records Contacts at 550.8/154.6 KiB and Calculator at 1158.5/302.4 KiB raw/gzip initial.
- [x] `npm run audit:security` has no unresolved high or critical production vulnerabilities.
- [ ] The private-key material removed in `db20ed2e` has been rotated or revoked and downstream use audited; Git history is not rewritten.
- [x] `npm run test:jobs` passes the JOB-03 TypeScript, provider, static-migration, and repository-security checks at 8 files/144 tests.
- [x] `npm run test:worker`, `npm run typecheck:worker`, `npm run build:worker`, and the built CLI smoke pass; worker coverage is 12 files/134 tests.
- [x] JOB-01's five migrations pass `npm run test:jobs:db` in the isolated logged-PGMQ CI harness on both supported database images.
- [x] JOB-02's six-migration matrix plus worker typecheck/tests/build/CLI, strict service-role guard, and non-root Docker build pass in the dedicated CI workflow.
- [x] JOB-03 `npm run test:email-provider`, updated `npm run test:jobs`/`npm run test:worker`, adapter/webhook/repository tests, typecheck/lint/build, and seven-migration real-PGMQ matrix pass at the checkpoint commit in Background Jobs run 29723041212.
- [x] `npm run test:portal:smoke` passes for authenticated portal routing.
- [x] `npm run test:portal:browser` passes for drawing/workbench browser smoke without silent full-suite skips.
- [x] `npm run test:portal:performance` passes route timing budgets with exactly five authenticated CI repetitions.

### Critical Staff Workflows

- [ ] Login, logout, no-access, and access-status flows verified.
- [ ] Contacts list, create, update, and detail flows verified.
- [ ] Projects list, create, detail snapshot, journey/stage/state filtering, and Project Work flows verified.
- [ ] Work Queue and Dashboard preview verified for one-row-per-project precedence, normal commands, durable confirmation, stale/retry states, responsive containment, and separation from personal reminders.
- [ ] Admin confirmation correction verified as append-only with required reason and explicit review signal.
- [x] Portfolio rollout verified for replay/partial repair, fresh stage timing, Paid/Archived handling, all-project completeness, retired legacy grants, and no customer communication or specialist mutation.
- [ ] Calculator estimate creation, update, versioning, summary, and warnings verified.
- [ ] Calculator Brain draft/list/history, typed validation, diff/impact, confirmation, publish audit, and rollback verified against a migrated authenticated environment.
- [ ] A post-publish calculator save records the exact response version in `outputs.configVersions.costingControl` and `estimates.costing_config_version_id`; a pre-publication save retains the hashed legacy snapshot.
- [ ] Estimate edit locks verified for sent, accepted, and declined quote-backed estimates.
- [ ] Local-first estimate and quote flows verified for pending, success, failure, retry, alias, and conflict states.
- [ ] Quote draft, refresh, preview, PDF, send, resend, accept, decline, and revise flows verified.
- [ ] Deposit invoice creation, send, retry/failure, public token, and PDF flows verified.
- [ ] Job-pack generation, reuse, PDF, and powdercoating override conflict flows verified.
- [ ] Design request creation and Design List spreadsheet editing verified.
- [ ] Schedule Board assignment, reorder, move, unschedule, and refresh persistence verified.
- [ ] Schedule Gantt date alignment, range controls, crew collapse, and bar behavior verified.
- [ ] The retained Site Visits workflow remains hidden from normal navigation and outside work items; only the approved active-V2 `site_visit` stage exposes the direct booking/confirmation link, with completion manual and no automatic stage or Schedule side effect.
- [ ] Running Jobs spreadsheet edits, project-running-job fact handling, and schedule-owned read-only fields verified.
- [ ] Design workbench edit, save, reload, object-first geometry, plan, 3D, and fallback visibility verified.
- [ ] Project page Overview Project Work + notes: open project lands on the internal `activity` key labelled Overview, the one V2 work surface preserves stale/retry/Waiting/Closed/Archived/idempotent behavior, notes compose / edit-own / delete-own succeed, non-author staff sees no edit/delete affordance, admin can edit/delete any note, and soft-deleted notes hide on reload.
- [x] Project page Overview current-design/commercial card: deterministic unit, route, component, and browser coverage proves accepted > sent > draft > estimate precedence, declined-only history, multiple estimates/modules, exact quote-source links, new-lead empty state, and explicit missing-source/missing-price states.

### Security And Data Boundaries

- [ ] Staff APIs use `requireStaffSession` or `requireStaffContext`.
- [ ] Admin APIs use `requireAdminSession` or `requireAdminContext`.
- [x] Public quote and invoice flows remain token-bound and hash-checked.
- [x] Service-role Supabase access is server-only and limited to documented owner flows.
- [ ] `npm run service-role:changed` is included in handoffs that touch service-role Supabase access.
- [ ] Browser UI does not add direct table writes outside API, query, local-first, or approved spreadsheet adapters.
- [ ] `npm run browser:supabase:changed` is included in handoffs that touch browser-facing Supabase access.
- [x] Schedule V2 writes go through staff API routes and `schedule_v2_*` RPC commands.
- [x] Quote, invoice, PDF, email, and job-pack side effects go through domain helpers.
- [ ] Migrations are ordered forward migrations; old applied migrations are not edited without explicit direction.
- [ ] RLS and grants are documented for changed tables or RPCs.
- [ ] Raw tokens, token hashes, service-role keys, and private artifact access do not reach client components, logs, PDFs, or public props.
- [ ] Background-job queue messages contain only `jobId` and `contractVersion`; frozen payloads remain private and every worker payload read or mutation is lease-fenced through a service-role RPC.
- [x] All seven current JOB-01/JOB-02/JOB-03 migrations pass the isolated database contract on both supported real-PGMQ images.
- [ ] Complete normal shared-environment deployment review before any producer, active worker, or rollout is enabled.

### Performance And UX

- [ ] Schedule page meets bundle and route timing budgets.
- [x] Project list navigation meets the new 100/500 ms five-run p75 contract; project detail remains green.
- [x] Contacts list meets the 100/500 ms five-run p75 route timing budget.
- [ ] Heavy views are lazy-split by actual workflow boundaries.
- [ ] Board, Gantt, Site Visits, design workbench, and spreadsheet surfaces remain responsive under realistic data volume.
- [ ] Loading, empty, error, pending, retry, stale, and locked states are visible and useful.
- [ ] Core workflows are manually checked at desktop and mobile/tablet widths where staff may use them.
- [ ] Browser tests or manual screenshots confirm high-risk canvas/SVG/3D views are nonblank and correctly framed.

### Maintainability

- [ ] Large files have an owner and a decomposition plan before major feature work continues in them.
- [ ] Warning or critical files touched by feature or bugfix work include one small extraction, or a handoff note that names why extraction was deferred and what the next safe extraction is.
- [ ] Parallel or dirty-tree work uses `npm run worktree:status` with `WORKTREE_OWNER_PATTERNS` before editing, `npm run architecture:changed` before handoff, and `npm run worktree:changed:strict` for explicit local lane enforcement.
- [ ] `npm run architecture:changed` is included in non-trivial portal handoffs, including worktree ownership, dead-code changed reporting, and changed-file architecture checks.
- [x] Portal Quality runs `npm run architecture:changed` as a PR-aware advisory report against base/head changes.
- [x] Portal Quality runs `npm run architecture:changed:strict` as a PR-aware advisory report without blocking legacy debt; local dirty-tree strict runs require declared ownership patterns.
- [ ] `npm run files:report` is reviewed before expanding warning or critical files.
- [ ] `npm run files:changed` is included in handoffs that touch warning or critical files.
- [ ] `npm run root:compat:changed` is included in handoffs that touch root compatibility paths before portal SaaS extraction work continues.
- [ ] `npm run dead-code:changed` is used directly for focused deletion, dependency, or cleanup work that needs the dedicated dead-code report.
- [ ] `npm run dead-code:changed:strict` is used for local cleanup/tooling verification when new unused files should be blocked without blocking legacy debt.
- [x] Portal Quality runs `npm run dead-code:changed` as a PR-aware advisory report against base/head changes.
- [ ] Source-of-truth boundaries are preserved for costing, geometry, durable jobs, email-provider transport, schedule, local-first, quotes, invoices, and job packs.
- [ ] Geometry/costing migration preserves the target path: object-first design intent -> `@sp/geometry` solved physical model -> geometry-derived quantity takeoff -> `@sp/costing` commercial input and pricing -> estimates, quotes, invoices, and job packs.
- [ ] Compatibility and legacy fallback paths are isolated, named, and tested.
- [ ] Commercial parity coverage compares `calculator_compat` and `workbench_solved` before live estimate or quote pricing consumes the workbench-solved path.
- [ ] No new duplicate workflow rules are added in components when an owning domain helper or definition exists.
- [ ] New tests land at the owning layer first, then broaden only when blast radius requires it.
- [ ] Docs are updated when behavior, data flow, source-of-truth boundaries, test strategy, or known risks change.

## Workbench Solved Pricing Go/No-Go

Do not enable `PORTAL_ESTIMATE_PRICING_SOURCE=workbench_solved` until every item below has named evidence. Estimate persistence is the only live switch point; geometry remains owned by `@sp/geometry`, costing remains owned by `@sp/costing`, and quote/invoice/job-pack flows stay on saved estimate or quote-version boundaries.

### Readiness Gates

- [ ] The rollout readiness report has `eligibleToEnable: true` and an empty `blockingGateCodes` array.
- [ ] All gate codes pass: `workbench_solved_ready`, `quantity_takeoff_owned`, `commercial_parity_stable`, `estimate_persistence_source_explicit`, `estimate_lock_boundary_preserved`, `local_first_boundary_preserved`, `downstream_pricing_boundary_preserved`, and `rollback_to_calculator_live_confirmed`.
- [ ] Workbench commercial evidence shows source `workbench_solved`, trust `ready`, zero blocking diagnostics, quantity takeoff source `package_geometry` or `solved_geometry_spine`, and `fallbackPricingSource: null`.
- [ ] `calculator_compat` versus `workbench_solved` parity reports are `status: match`, include the parity-critical baked fixtures and representative saved estimate snapshots, and show `counts.blockingDifferences === 0`.
- [ ] Authenticated manual QA evidence exists for estimate save/update/duplicate, local-first pending/failure/retry/conflict states, estimate locks, quote refresh, public quote, deposit invoice, PDF, and job-pack paths.
- [ ] The final decision record includes exact command outputs, manual QA artifact links or IDs, rollout approver, selected flag value, selected time, and rollback owner.

### Rollback And Immutability Checks

- [ ] Rollback is documented as an explicit switch to `PORTAL_ESTIMATE_PRICING_SOURCE=calculator_live`, followed by redeploy/restart and a calculator-live estimate save smoke confirming new rows record `pricing_source: calculator_live`.
- [ ] Pre-enable, post-enable, and post-rollback snapshots compare row counts and stable IDs for `quote_versions`, `quote_line_items`, `quote_send_logs`, `deposit_invoices`, `deposit_invoice_send_logs`, `file_artifacts`, `job_pack_generations`, and `job_pack_sheet_overrides`.
- [ ] Historical quote totals, invoice totals, generated artifact IDs, public token hashes, send-log IDs, and job-pack generation IDs are unchanged across enablement and rollback.
- [ ] Raw `commercial_design_input` is absent from quote versions, public-token responses, PDFs, emails, invoice payloads, and job-pack outputs.
- [ ] Blocked `workbench_solved` attempts return `409 ESTIMATE_PRICING_SOURCE_BLOCKED`, emit an audit event with gate codes, and leave estimate rows unchanged.

### Post-Enable Signals

- [ ] Estimate save logs show expected `pricing_source` distribution for new saves: no invalid source values, no unset selected source, and no calculator fallback while reporting `workbench_solved`.
- [ ] Audit events reconcile with estimate mutations: each successful workbench save has `estimate.pricing_source_saved`, each blocked attempt has `estimate.pricing_source_blocked`, and blocked attempts have no paired estimate mutation.
- [ ] Error checks show no spike in `ESTIMATE_PRICING_SOURCE_BLOCKED`, `ESTIMATE_LOCKED`, quote refresh failures, public token failures, PDF generation failures, invoice send failures, or job-pack generation failures after enablement.
- [ ] Local-first conflict checks show blocked creates do not register durable aliases, queued quote/design-request actions remain queued or conflicted until a durable estimate save succeeds, and retry does not silently switch to `calculator_live`.
- [ ] Downstream artifact checks show new draft quote refreshes use saved estimate/quote-version boundaries and historical sent/accepted/declined quote versions remain unchanged.
- [ ] Failure modes with named owners are recorded for blocking diagnostics, parity drift, app-local takeoff source, missing source metadata, lock bypass, local-first alias drift, downstream mutation, audit write failure, and rollback smoke failure.

## Highest Leverage Tasks

Keep this ordered list current as work lands.

1. Make Project Pages Speed the highest product lane: preserve the current portal UI, keep PPS-01 and PPS-03 green, complete PPS-02 against the 100/500 ms warm ceiling, and defer creation complexity until the everyday project journeys are green.
2. Keep the unchanged Contacts/Calculator bundle ceilings and current green route-optimization evidence; retain the earlier Overview handoff exception as historical truth rather than authority to raise budgets.
3. Keep the exact production target guard and pagination-aware GET-only browser proof as a release check; provision production-only credentials explicitly when dispatching it in CI rather than reusing staging credentials.
4. Restore any quality gate that blocks the project-speed lane before adding an optimization; when the failure is in a hotspot, fix it at the owning layer rather than adding another caller workaround.
5. Close the repository key incident in parallel by rotating or revoking the compromised credential and auditing downstream use; do not rewrite Git history.
6. Preserve JOB-03's green seven-migration, contract/integration, and worker-container CI signal, then continue JOB-04 through JOB-08 in a separate backend worktree; require shared-environment deployment review before enabling any producer or active worker rollout.
7. Keep manual quote/invoice/public-token/job-pack, Schedule V2, and Design Workbench edit/save/reload QA visible in release checks with staff credentials and compatible data.
8. Advance the geometry/costing migration without switching live pricing: extend package-owned physical takeoff in `packages/geometry`, compare `calculator_compat` and `workbench_solved`, and keep compatibility retirement explicit.
9. Make hotspot maintainability part of ordinary work: use `npm run files:report` to choose the next owner surface, and use `npm run files:changed` plus a decomposition note whenever warning or critical files are touched.

## Parallel Work Lanes

Parallel work is encouraged when ownership is clear and file overlap is low. Read `docs/parallel-work-guardrails.md` before running concurrent lanes across shared contracts, packages, apps, docs, or workbench migration areas.

| Lane | Scope | Safe In Parallel With | Avoid Touching |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Quality gate repair | Current failing tests, lint guards, build blockers. | Security/deps, docs tracker updates, isolated feature fixes. | Broad refactors not needed for the failure. |
| Security/dependency audit | Production dependency upgrades, audit remediation, residual-risk notes. | Contacts/projects, schedule performance, workbench fixes. | Large feature behavior changes unless required by upgrade. |
| Contacts/projects env boundary | Contact APIs, contact/project server data helpers, project snapshot tests. | Schedule, workbench, style isolation, security/deps. | Quote/invoice side-effect helpers unless failure crosses that boundary. |
| Project pages speed | Projects Index query/cache/navigation, Project Detail summary/snapshot/opening/Back, current project-tab preload and bundle boundaries, and authenticated performance evidence. | Backend-only durable-job work, schedule, workbench, and security remediation that do not touch the same shell or project workflow files. | Shared portal UI owners without explicit visual-change approval; quote/invoice/job-pack behavior; broad shared-shell refactors; speculative caching without a measured journey. |
| Project Work portfolio | `lib/projects/workItems`, Work Queue/Dashboard/Projects/Overview presentation, the exact rollout migration, legacy task retirement, focused tests and owner docs. | Workbench, geometry/costing, durable jobs, or isolated Schedule work that does not touch shared project projections. | Customer communication, Site Visit task integration, readiness inference, or unrelated specialist behavior. |
| Project Overview V2 | Overview route composition, route-owned Project Work extraction, command-centre/snapshot adapters, fixtures, responsive/accessibility checks, and owner docs under the approved handover. | Workbench, geometry/costing, durable jobs, and isolated specialist work that does not touch project projections or shared portal primitives. | Portal-wide restyle, shared-token replacement, Site Visit tasks, customer sends, specialist side effects, or browser-derived readiness/commercial truth. |
| Schedule performance | `/staff/schedule`, schedule queries, schedule CSS, route timing, bundle split. | Contacts/projects, workbench, security/deps. | Design workbench, quote/invoice, unrelated portal shell. |
| Design workbench | `apps/portal/components/drawings`, `apps/portal/lib/drawings`, workbench fixture/browser gates. | Contacts/projects, schedule, security/deps. | Calculator or costing files unless needed by explicit geometry contract. |
| Geometry/costing migration | `packages/geometry`, `packages/costing/src/commercial`, commercial parity adapters, and docs for the target boundary. | Design workbench, calculator decomposition, docs tracker updates. | Live estimate/quote pricing, public outputs, or job-pack pricing unless an explicit rollout task is in scope. |
| Quote/invoice/job packs | Quote/invoice/job-pack domain helpers, token routes, PDFs, email side effects. | Schedule performance, style isolation, docs updates. | Contacts/projects internals unless working on a documented handoff. |
| Durable background jobs | `apps/worker`, `packages/jobs`, `packages/email-provider`, the provider webhook/repository, seven JOB-01/JOB-02/JOB-03 migrations and tests, isolated database harness, and Background Jobs CI workflow. | Docs tracker updates and domain lanes that do not yet migrate a producer or handler. | Existing quote/invoice/job-pack/email/automation ownership until a named later JOB explicitly owns that integration. |
| Style isolation and portal shell | Shared layout, surface styles, PageHeader, portal shell tests. | Security/deps, contacts/projects, schedule. | Feature behavior and workflow-specific CSS unless required by isolation test. |
| CI/typecheck/tooling | Scripts, workflows, typecheck, docs guard, command docs. | Most domain lanes. | Domain behavior changes unless a gate requires a small fix. |
| Large-file decomposition | `CalculatorGridClient`, `ModuleViewsCard`, `ScheduleClient`, `Geometry3DViewport`, `PlanViewport`, and files reported by `npm run files:report`. | Only lanes that do not touch the same files. | Active feature fixes in the same large file. |
| Hotspot bugfix with extraction | Current failing behavior inside a warning or critical file, plus the smallest owner extraction that makes the fix easier to test. | Docs tracker updates, unrelated security/deps, isolated feature work outside the same files. | Broad cleanup, formatting, or moving behavior that is not needed to stabilize the bug. |

## Canonical References

Use these docs as routing references. Do not copy their full rules into this tracker.

| Area | Read First |
| ------------------------------------------ | ------------------------------------------------ |
| Agent protocol | `docs/agent-playbook.md` |
| Path ownership and doc update triggers | `docs/change-routing.md` |
| Repo and app boundaries | `docs/architecture.md` |
| Maintainability principles | `docs/maintainability-principles.md` |
| File decomposition and ownership | `docs/file-decomposition-and-ownership.md` |
| Code retirement and bloat control | `docs/code-retirement-and-bloat-control.md` |
| Whole platform workflow | `docs/platform-workflow.md` |
| Commands, QA gates, browser tests | `docs/testing-and-qa.md` |
| Auth, env, Supabase setup | `docs/environment-auth-supabase.md` |
| Staff/admin/public route contracts | `docs/staff-api-auth-contracts.md` |
| Supabase tables, RPCs, migrations, RLS | `docs/supabase-schema-map.md` |
| Contacts, projects, estimates, calculator | `docs/projects-contacts-estimates-calculator.md` |
| Local-first queues and working copies | `docs/local-first-sync.md` |
| Quotes, invoices, public tokens, job packs | `docs/quotes-invoices-job-packs.md` |
| Automation, email outbox, audit events | `docs/automation-email-audit.md` |
| Costing and geometry source of truth | `docs/costing-and-geometry.md` |
| Design workbench | `docs/design-workbench-architecture.md` |
| Parallel or cross-area work | `docs/parallel-work-guardrails.md` |
| Schedule | `docs/schedule.md` |
| Design List | `docs/design-list.md` |
| Running Jobs | `docs/running-jobs.md` |
| Security, privacy, audits | `docs/security-privacy-quality.md` |
| Lessons and durable guardrails | `docs/decision-log.md` |

## Update Rules

When updating this tracker:

- Update `Last updated` at the top.
- Move checklist items only after verification or a clearly documented finding.
- Add exact commands and results to the relevant row or change note.
- Keep status labels simple: Green, Yellow, Red, Unknown.
- Prefer compact factual notes over future-plan prose.
- If a finding creates a reusable rule, add or update `docs/decision-log.md`.
- If implementation changes behavior or ownership, update the canonical owner doc as well.
- Keep this file ASCII and link to repo-relative paths.

## Change Notes

### 2026-08-06

- Replaced heading/skeleton acceptance with route-specific final-frame and continuity checks, including a Dashboard revisit after more than two minutes idle. Common staff/admin destinations now own exact data-free frames; live values settle inline, heavy specialist modules retain their boundaries, and the already-open app can use those frames for soft offline navigation. A versioned static-only worker excludes documents, HTML, RSC/Flight, APIs, and protected responses. Cached role/theme state is presentation-only, owner data waits for live verification, and logout/owner/access changes clear per-owner local state. Production build, bundle, authenticated performance, and browser evidence for this combined ref remain the promotion gate; offline hard refresh/new-tab startup is explicitly outside this slice.
- Revalidated the current portal in an optimized production build and found Projects-to-project useful-content p75 at 1,329 ms because the project shell looked only at retired canonical list query keys while the current Projects Index owned a combined response key. The cache lookup now promotes the freshest matching current-user Projects-index project/contact row first and retains the old keys only as fallback. Sidebar links also disable automatic viewport prefetch and explicitly preload route/data from hover, focus, pointer-down, or touch. Exactly five authenticated production repetitions passed at 14/19 ms feedback/useful p75, cold Project Detail remained inside its unchanged 2,699 ms guard at 2,663 ms useful p75, all project samples had no overlay or long task, focused coverage passed 5 files / 42 tests, and the optimized portal build passed. One extra attempted repetition failed the unrelated cold Schedule per-run shell guard at 10,426 ms; a clean replacement run passed. The target database still reports `PGRST205` when the browser tries to retain Web Vitals, so the JSON journey artifacts are the available evidence until that performance-metrics schema is visible to PostgREST.

### 2026-08-01

- Deployed the dedicated Project Close journey and admin stale-Enquiry review as production deployment `dpl_5W9xowMaBW1jTKhn56EbuZC6H6ZS`. The exact bulk-close migration SHA-256 `f04793197301526f4c0b5d15e434bbede43ef51ace73f48b903bdf769d10a8ef` was rollback-rehearsed and applied to staging and production; RLS, authenticated-only execution, anonymous denial, 1,149/1,149 production state coverage, and zero close receipts passed. The first live GET-only smoke caught the missing production V3 Projects read prerequisite; its already-reviewed exact hash `4297d1acd87d9ec523b71d13e962379fe8a47f4c12393d9bb6ad028e75a00c0b` was rollback-rehearsed/applied, after which Projects, Dashboard, Work Queue, and an Active Overview Close dialog passed 4/4 with all application mutations blocked. The final inactivity read found three candidates; none were closed pending exact-list approval.
- Made normal Schedule Board persistence silent and placement-led: beginning, middle, end-of-lane, cross-crew and unschedule moves keep the exact optimistic position while server-owned Schedule V2 commands settle, with bounded authoritative reconciliation and at most one correction. Routine saving/saved banners and toasts are removed from Board; only definitive rollback or unverifiable truth renders one inline Retry or Refresh notice. The production portal build passes, `npm run test:portal:schedule` passes 54 files / 413 tests plus the bundle budget (659.8/181.8 KiB initial raw/gzip; 248.6/71.7 KiB lazy), and both fixture and authenticated-context Chromium runs pass all six synthetic scenarios with zero staff writes. Read-only rendered checks at 1600x1000, 1440x900, 1280x800, 1024x900, 768x1024, 390x844 and a 200%-zoom equivalent confirm contained layouts, silent normal state and one actionable exceptional notice.
- Refined the repository-local Schedule Board and Gantt operational presentation without changing Schedule V2 contracts, commands or data. A shared presentation owner aligns crew forecast workload, plan commitment and factual attention signals across views; Board cards and lanes have stronger timing/sequence hierarchy, Gantt bars and crew rows retain readable identity, and the narrow-width Gantt is an explicit read-only agenda. `npm run test:portal:schedule` passes 54 files and 409 tests plus its bundle gate, the 11-scenario synthetic Chromium gate passes every requested operational width and 200% zoom with zero staff writes, lint/typecheck and the isolated production build pass, and authenticated direct renders verify Board and Gantt using existing records only. Near-simultaneous multi-staff edit protection remains the explicit server-contract deferment.

### 2026-07-31

- Rehearsed the exact Project Work portfolio migration against the real production portfolio inside a rollback transaction after confirming completed physical backup `1252785067`, then applied the unchanged SHA-256 `a9e91e48e0a894bbe9201cc39c7ba5e83c4d33b9d8912c0b6d369bf058755ef3` once. Production postflight found 1,151/1,151 V2 markers and operational states, one rollout ledger row, 1,151 rollout events, 1,109 Active and 42 Closed projects, and zero active Call/Site Visit/legacy-review work. Legacy table counts remained exactly 178 tasks, 8 follow-ups, 178 checks, and 70 manual actions. No email, quote, invoice, schedule, payment, or customer communication was triggered.
- Merged Project Work release PR `#40` as `d63d57ad` and verified the live production alias resolves to the exact ready Vercel deployment for that commit. Added a manually dispatched `main` smoke that requires separate production-only credentials, accepts only the exact production portal origin and Supabase project ref, and blocks every application request other than `GET`/`HEAD`/`OPTIONS`.
- Completed the exact production smoke locally with a short-lived labelled staff QA login that was hard-deleted immediately afterward. Projects/Dashboard, one real Overview, and the 779-entry paginated Work Queue passed with every non-read application request blocked; the first run correctly exposed a browser assertion that confused the 100-row rendered page with total queue inventory, and the corrected test now verifies the first and next bounded pages. No customer/project/commercial/task/schedule/payment record changed, and no QA auth or `portal_users` residue remained.
- Isolated the portal Preview environment onto Supabase staging while retaining the production Supabase values only for the Production environment. Rehearsed the exact `20260731000002_project_work_portfolio_rollout.sql` file in a rollback transaction, then applied its unchanged SHA-256 `a9e91e48e0a894bbe9201cc39c7ba5e83c4d33b9d8912c0b6d369bf058755ef3` to positively identified staging. Postflight found all 11 projects marked V2 with operational state, one durable rollout ledger row, 11 portfolio-rollout events, zero active Call/Site Visit/legacy-review work, and unchanged zero-row legacy task/action tables. Staging readiness denied anonymous access as required. The protected Preview then passed three authenticated read-only browser checks covering Projects/Dashboard, Work Queue, and one real Overview with every application mutation request blocked.
- Rehearsed the exact Pipeline Accountability read migration in a staging rollback transaction, restored and verified its SHA-256 `4297d1acd87d9ec523b71d13e962379fe8a47f4c12393d9bb6ad028e75a00c0b`, then applied that file only to positively identified staging. Catalog/readiness checks proved authenticated-only V3 execution and anonymous denial. Authenticated read-only RPC proof returned all 11 staging projects with complete operational/effective-state context, all 11 for the unassigned-owner filter with zero mismatches, and nine Work Queue rows. An isolated optimized staging build then rendered Projects, Work Queue, Dashboard, explicit stage correction, and Overview at desktop and 390 x 844 with aligned owner/action/reason/When language and no document overflow or application writes. The exact Vercel Preview deployment `dpl_DqEDdq1UJTdDN5G8t1XMnN9puNYR` built successfully; authenticated protection-bypassed GETs returned 10 active unassigned Projects rows with zero owner mismatches, nine Work Queue rows, five Dashboard preview rows, and a readable Overview Project Work state. A temporary deployment-scoped share link then enabled the final authenticated interactive smoke: Projects owner filtering, Work Queue search and When filtering, Dashboard parity, Overview accountability, correction-review wording, and desktop/390 x 844 overflow checks all passed without business-data writes. The share link and dedicated staging QA auth sessions were revoked afterward, Preview protection was reverified, and production remained unchanged.
- Corrected the Portal Performance CI build path so its production server starts the build it just produced. The gate now reaches authenticated measurement; its remaining missing-Job-Pack prerequisite, the Portal Quality PDF timeout/current-copy fixture failures, and the marketing Lighthouse mobile failure reproduce in pre-rollout PR runs and are not Project Work regressions. No customer/shared record was created to satisfy those unrelated baselines.
- Prepared the project-linked Design Booklet Workbench release: one autosaved draft per project, private direct image uploads, project restoration and return navigation, and saved-asset PDF publication through a short-lived signed URL. The exact `20260731_000001_project_design_booklets.sql` file passed a rollback rehearsal and was applied to positively identified production after confirming a completed physical backup. Postflight proved two RLS-enabled tables, six policies, eight authenticated grants, zero anonymous grants, both timestamp triggers, the private bucket contract, and zero booklet, asset, or Storage object rows. The exact SHA-256 was `3af810d27c9406f2ba125cb74c0af6c09386b0ce703ba5bd2539c431d646a14e`; the date-only ledger remained untouched and no blanket migration command or repair was used.

### 2026-07-30

- Deployed the clean Project Work/Overview release merge `c9e73651`, then applied the exact reviewed `20260729_000002`, `000003`, and `000004` files individually to positively identified production after confirming a completed physical backup and a quiet write window. Postflight found nine RLS-enabled tables, authenticated-only queue execution, canonical relationships, zero markers, states, work items, work-item/state events, confirmations, command receipts, or repair signals, and an untouched colliding migration-ledger state. Authenticated read-only Work Queue, legacy snapshot, Command Centre, and Overview-page GETs returned `200`; marker count was zero at the cutover check because no project was migrated or backfilled.
- Production verification found the complete authenticated project snapshot inheriting a public revalidation header while adjacent Project Work reads were already `private, no-store`. Deployed snapshot-cache hotfix merge `809f2c5e` applies `private, no-store` to success, authentication, validation, not-found, and server-failure responses and adds focused branch coverage. The authenticated production `200` and unauthenticated `401` responses were rechecked with the exact header after deployment. No schema, data, lifecycle, commercial, Schedule, Workbench, or task behavior changed.
- Completed the production-refusing new-V2-project command smoke against one clearly labelled synthetic staging project. Authenticated creation initialized `OPEN r1`; `BLOCK` committed `r2`; the byte-equivalent command-ID replay remained `r2` without a duplicate event; and supported V2 archive cleanup produced `ARCHIVED` plus `CANCELLED r3` and removed the project from Work Queue. Work-item, Command Centre, snapshot, integrity, and rendered Overview reads agreed. The synthetic QA role was restored to `staff`. No confirmation, email/outbox, quote, invoice, legacy task/follow-up, Site Visit, Schedule, Running Jobs, repair, legacy residue, real customer, or production data was created or changed. The archived synthetic project/contact and append-only command evidence intentionally remain in staging because that V2 command smoke did not invoke the separate destructive admin project-delete route.
- Positively identified the linked healthy staging Supabase project and separately guarded the production project. Rehearsed exact `20260729_000003` and `000004` SQL inside rollback transactions, then replay-applied only those files. Pre/post readiness passed; catalog verification proved one canonical cascade relationship per marker/state table, the expected V3 queue and guarded classifier/migration bodies, authenticated execution, and denied anonymous execution. The later authenticated GET-only smoke returned `200` for Work Queue, a disposable synthetic legacy-project snapshot, and its Command Centre, with fresh/ready UI states, the legacy boundary preserved, and zero browser writes. The exact synthetic contact/project was removed afterward. No existing project, Contacted record, customer communication, quote, invoice, schedule, or production data was changed.
- Supabase CLI treats every `20260729_00000N_*` filename as remote version `20260729`, so the sparse staging ledger cannot safely record `000003` and `000004` separately. Direct exact-file SQL was used; blanket `db push`, `migration up`, and `migration repair 20260729` remain prohibited until migration naming/ledger compatibility is fixed.

### 2026-07-29

- Added the repository-local V2 Work Queue/legacy-review slice: a staff-wide one-row-per-project queue, Dashboard preview, canonical specialist composition, semantic work commands, append-only admin confirmation correction, read-only Contacted classification without linked customer contact fields, and guarded one-project reviewed migration. Personal reminders remain separate; Site Visits remains hidden/manual and outside project work; Project Overview was not redesigned. `20260729_000004` is unapplied and the Contacted cohort is unchanged pending focused and repository verification.
- The first Project Work V2 staging migration rehearsal exposed a PostgREST schema-cache miss (`PGRST205`/`PGRST200`) that made every individual project snapshot fail before legacy/V2 classification. Project, Command Centre, Schedule, exceptions, Running Jobs, and Work Queue readers now obtain V2 marker/state data through direct bounded server owners instead of embedded relationships. Authoritative queue inventory fails closed when missing or truncated. Forward migration `20260729_000003_project_work_items_v2_schema_cache.sql` canonicalizes the named cascade project foreign keys, and both `000003` and `000004` notify PostgREST after commit. A production-refusing read-only readiness command plus the named Project Work contract/fixture gate are repository-local. Cached rows are read-only and absent V2 contracts fail closed. Authenticated legacy-read and disposable new-V2 command smokes now pass; production promotion remains separately controlled.
- Implemented the repository-local, new-project-only V2 work model without applying or deploying its migration and without mutating the Contacted backlog. It adds Auckland-calendar state/work/confirmation commands, lead and quote cadences, one-row-per-project queue, one-way compatibility, the current Overview adapter without redesign, admin archive, hidden/manual Site Visit handling, and Schedule/Running Jobs specialist ownership. Local evidence passes the 12-test executable PGlite migration contract, 31 focused cross-domain tests, and 47 focused Overview/projection/route tests. Rollout still requires database-first promotion, authenticated safe QA, full repository gates, calendar coverage beyond 2027, and explicit review of any existing-project migration.

### 2026-07-28

- Implemented the repository-local Option B commercial-trust slice without applying its migration or triggering a real send, acceptance, invoice, or production write. Estimate and quote creation now use stable client intents; quote edits and delivery preparation are revision-bound and transactional; prepared requests are frozen privately with provider checkpoints and exact retry identity; acceptance atomically creates or reuses one deposit invoice while delivery failure remains recoverable. Staff UI distinguishes local, pending, stale, prepared, provider-confirmed, retryable, and staff-attention states, and the public accepted page reports the actual invoice-delivery outcome.
- Added a gated customer-data-free commercial recovery fixture that renders the production quote detail and immutable prepared-delivery dialog at desktop and 390px. Manual browser evidence covers retryable and terminal states, read-only content, no console errors, focus return, responsive containment, and 44px mobile targets. Deterministic quote PDFs were rendered with Poppler and inspected across simple, multi-page, long-description, and long-terms cases; all continuation headers, totals, acceptance wording, terms, page numbers, and footers were separated without clipping.
- Local evidence passes the full portal and marketing Vitest suites, focused commercial/route/fixture tests, repository typecheck/lint, zero-vulnerability production audit, PostCSS 8.5.18 resolution, isolated Next.js 16.2.11 portal and marketing builds, docs impact, architecture, ownership, service-role/browser-Supabase inventories, strict new-file dead-code reporting, and diff whitespace checks. The ordered migration remains a static contract because no disposable local PostgreSQL/Supabase runtime is available; the connected authenticated environment truthfully returns a retryable quote-detail failure until that migration is promoted through the normal reviewed path.

### 2026-07-20

- Completed the repository implementation of Project Operational Command Centre Stage 2 from committed baseline `8770198f`: canonical ownership and source-linked action records, selector/conflict rules, transactional command APIs, Auckland business due dates, project/dashboard UI, audit history, automation cutover, legacy writer retirement, and the server-owned Schedule projection are implemented locally. Evidence passes 335 project tests, 18 fixture Playwright checks, full typecheck/lint, an isolated 66-page production build, all unchanged bundle ceilings, and architecture/docs/dead-code checks. Stage 2 remains Yellow because no local PostgreSQL/Supabase executable migration target is available and `PORTAL_TEST_EMAIL`/`PORTAL_TEST_PASSWORD` are not provisioned for authenticated real-project Playwright.
- Completed Project Operational Command Centre Stage 1 in commit `8770198f`, now present in current repository history: Overview owns a strict server-read commercial/design summary and truthful pending, refresh, stale-failure, initial-failure, and access-ending states. At that checkpoint it retained customer context, notes, tasks, Details, and specialist workflows; later project-shell work consolidated Details into Overview, replaced the legacy estimate surface with Calculator, grouped Quotes/Invoices in Commercial, and retired the Emails UI. The legacy three-query Activity snapshot bar and unsafe source/price fallbacks were deleted. All nine data-free scenarios and the 390px browser check passed; 322 project tests, 9 fixture performance checks, repository typecheck/lint, an isolated 64-page production build, unchanged bundle assertions, docs impact, architecture, file-decomposition, and strict dead-code checks also passed. Authenticated smoke/performance remained unavailable at that checkpoint because local test credentials were not provisioned; the user's existing port-3001 portal server was not stopped.
- Completed the repository-local JOB-03 checkpoint: `@sp/email-provider` owns exact Resend identity/bytes and signed-envelope parsing, the worker has the required ten-point hard-crash matrix plus one business-finaliser lost-return boundary but no registered commercial handler, and the seventh migration owns minimal signed-webhook reconciliation plus lease-fenced local acceptance/message-ID quarantine. Background Jobs [run 29723041212](https://github.com/velt-design/sanctuary/actions/runs/29723041212) passes all seven migrations on PostgreSQL 18/PGMQ 1.10.0 and Supabase PostgreSQL 17/PGMQ 1.5.1, plus contracts/integrations and worker artifact/container gates. Deposit-invoice, quote, and automation/outbox ownership remain request-bound. No shared database, production deployment, real email, producer, handler, or rollout was touched.
- Completed the JOB-02 repository checkpoint without enabling work: the Node 22 worker is dark by default, uses only strict service-role RPC contracts, appends a per-boot identity, fences handlers with lease/visibility heartbeats and abort settlement, bounds concurrency/retry/shutdown, exposes cached safe health and aggregate metrics, and supports one-shot, drain, reconciliation, and queue-health commands. The local 5-file/86-test job suite and 9-file/76-test worker suite pass with typecheck, lint, build/CLI, architecture, security, ownership, and dead-code gates. Background Jobs [run 29713940507](https://github.com/velt-design/sanctuary/actions/runs/29713940507) also passes the six-migration matrix on PostgreSQL 18/PGMQ 1.10.0 and Supabase PostgreSQL 17/PGMQ 1.5.1, plus the strict service-role and non-root container gates. No shared database, deployment, producer, handler, or real email was touched.
- Completed the repository-local JOB-01 durable-job foundation without claiming deployment: explicit effect allowlists and frozen per-job policy, typed/value-screened safe summaries, explicit safe read projections, exact queue-message validation and repair, bounded provider-idempotency recovery, a restart-safe effect identity boundary, and a Supabase-default-privilege-safe service-role RPC allowlist supplement the logged minimal queue and lease-fenced lifecycle. The local 4-file/71-test suite and repository gates pass. Background Jobs [run 29710584022](https://github.com/velt-design/sanctuary/actions/runs/29710584022) also passed the rollback-wrapped executable contract on PostgreSQL 18/PGMQ 1.10.0 and Supabase PostgreSQL 17/PGMQ 1.5.1. Docker was unavailable locally, the full historical migration chain was not independently bootstrappable, and the harness intentionally covered only minimal prerequisites plus the five JOB-01 migrations. At that checkpoint JOB-02 through JOB-08 and all shared-environment rollout evidence remained pending.
- Recorded the repository key incident: commit `db20ed2e` removed tracked private-key material, but history still contains it. Treat the credential as compromised until rotation/revocation and downstream-use audit are complete; do not rewrite Git history.

### 2026-07-19

- Moved the Contacts index from a route-blocking server read to a staff-authenticated API/query owner with current-user cache bootstrap, truthful pending/cached/fresh/failure/access states, and instant intent-preloaded navigation through the reusable portal-index controller. Contact cache changes now remain coherent across detail, canonical list, Contacts index, and every Projects index; CSV import is a lazy intent-preloaded workflow. Internal Contacts navigation now uses canonical `/staff/contacts`. The fresh Contacts bundle is 559.8/159.6 KiB initial and 120.6/19.2 KiB lazy, with about 0.7 KiB shared-shell growth. Portal Quality run `29678858906` locked five-run p75 at 35 ms feedback and 52 ms useful content, with no overlay or long task; the 100/500 ms ceiling remains unchanged.
- Moved the Projects index from a route-blocking server read to one staff-authenticated combined query, current-user cache bootstrap, explicit pending/cached/fresh/failure/access states, intent-only prefetch, and instant URL feedback without the global loading overlay. The persistent portal content boundary now renders the actual Projects frame and truthful updating state synchronously while Next settles the incoming route, and filter ownership starts client-side without awaiting request search params. Shared route gzip remains within the Slice 2 5 KiB allowance. Portal Quality run `29675363201` locked five-run p75 at 44 ms feedback and 76 ms useful content, with no overlay or long task; the 100/500 ms ceiling remains unchanged.
- Made routine project opening immediate from the signed-in user's cached project/contact summary, refreshed the canonical snapshot quietly, split tabs into intent-preloaded workflow boundaries, restored instant browser Back, and changed internal Projects navigation to `/staff/projects`.
- Locked five-run production evidence at p75 44 ms URL feedback and 60 ms useful content, with no overlay or long task; the project-opening regression ceiling is now 100/500 ms. Independent project/contact list reads start concurrently, while the root Projects route can stream its non-blocking pending shell.
- At that earlier performance checkpoint, Activity was still the default workflow and responsive Details was a separate surface; both were isolated behind truthful lazy boundaries while the workbench's Three-based viewport moved behind exact `3D Review` intent. The current shell has since replaced Activity with Overview and consolidated Details into Overview at every width. The recorded build kept Project Detail below its original combined cap while the Design Workbench gate accounted for deferred 3D code without increasing its previous allowance.
- Extracted the calculator costing request lifecycle so obsolete responses cannot replace newer results; final five-run p75 visible feedback is 47 ms and server costing remains authoritative.
- Memoized the workbench solved base by draft/project identity so UI-only selection, visibility, and viewport changes reuse geometry. Final fixture selection and Plan-to-3D useful feedback are 119 ms and 122 ms respectively.
- Completed the isolated cold Project Detail comparison without relaxing it: the 2,454 ms pre-Slice p75 gives a 2,699 ms guard, while the final recorded five-run p75 was 1,666 ms. A small authenticated direct-link summary makes the real header/tabs useful before the complete snapshot settles in the background.
- Request-scoped auth reuse removes repeated server-render access checks without crossing requests or users. A verified-claims/JWKS experiment made fresh-server cold routes slower and was reverted; the current boundary remains one verified user read plus one role read per render.
- Collapsed the complete snapshot's subordinate data into one auth-bound relationship read alongside the access-defining project/contact read. Removed the unused audit-event snapshot query and kept incomplete subordinate reads as truthful failures rather than fresh empty arrays.

### 2026-07-18

- Extracted the infill Results presenter, placed pieces and purchases before the compact tablet cutting diagram, added manufacturing-blocker browser coverage, and isolated the calculator trust suite onto a revisioned `calculator-multi-module` staging scenario. The 9-test calculator and 5-test infill Playwright suites pass; costing and `CostInputsV1` behavior are unchanged.
- Extracted the calculator configuration-section model and presenter from `CalculatorGridClient`, flattened ordinary field surfaces, and pinned workspace-aware field density to three columns at 1600px/1024px and two at 1366px/768px. Existing sections, fields, dependencies, validation focus, Basic/Advanced behaviour, pricing, Save, and drawing semantics are unchanged.
- Added a customer-price-led calculator preview and calm visual pass. The shared quote-pricing helper applies `1.25x` to pergola true cost ex GST, rounds that result, then applies GST; the calculator displays the same value without persisting it. Internal costing and separately priced blinds remain visible, while Save, Preserve/Reprice, quote totals, costing inputs, schemas, APIs, and workbench boundaries are unchanged.
- Added the grouped calculator module navigator: canonical per-pergola module identities, fresh Add, deep Duplicate with regenerated child IDs, Move without reordering, confirmed browser-draft-only Remove, validation badges, a sticky desktop rail, and a narrow modal launcher. Costing, Save/Preserve/Reprice, drawing, quote, API, schema, and workbench boundaries are unchanged.

### 2026-07-16

- Added the calculator trust slice: explicit current/stale/invalid/error result states, a persistent command bar, searchable project selection through the existing query layer, container-responsive scrolling, valid V2 scenario inputs, and authenticated calculator browser coverage. Pricing totals, save/reprice policy, APIs, schemas, and the workbench boundary are unchanged.

### 2026-06-11

- Restored docs health after `npm run docs:guard` failed on decision-log index drift: synced the 2026-06-03 Workbench House Forms index count with entries and escaped the `Sheet \| Plan \| 3D` mode-switch text inside the index table. Added the missing `Read First` navigation cue to `docs/environment-auth-supabase.md`, then reran the docs-only checks successfully. Existing dirty worktree files outside this lane were intentionally left untouched.

### 2026-05-30

- Refreshed docs current-state references after a docs-only cleanup pass: shipped PR-T5/T7/T8/T9 plan docs now read as retrospective rather than awaiting execution, dense docs have lightweight routing cues, and workbench commercial adapter wording now names `WorkbenchSolvedProject`.
- Baseline quick-doctor status changed to Yellow: the focused `apps/portal/lib/estimates/drawingEdits.test.ts` rerun still fails because the object-first deck draft expectation includes the removed `label` field. A later broad `npm run portal:doctor:quick:log` attempt during this docs-only cleanup was stopped after the Vitest worker hung; its log had already reported multiple non-doc portal failures across Supabase boundary, workbench import guard, drawing edits, workbench fixture/page, and estimate client tests. The docs-only cleanup does not change code or tests.

### 2026-05-10

- Promoted maintainability from advisory reading to default agent routing: hotspot work now reads `docs/maintainability-principles.md`, warning/critical file changes should include one small extraction or a named deferral, and handoffs should name the owner plus next safe extraction.

### 2026-05-04

- Strengthened Browser And Manual QA Coverage for final rollout confidence: the no-auth fixture browser gate keeps 7 passing fixture tests and 1 documented `portal-fixture` skip by design, the hipped/U-shape ready workbench-solved case now checks rollout diagnostics plus plan/3D topology, invalid fixtures remain a negative guard, and `docs/testing-and-qa.md` now carries the pricing-source rollout manual QA script.
- Made the no-auth fixture browser gate coexist with a normal portal dev server: Playwright now defaults the fixture harness to port 3011 and isolated Next dev output, `npm run portal:fixture-env` passed while port 3001 was occupied by normal portal dev, and `npm run test:portal:browser` passed with 6 fixture tests and 1 auth-backed smoke skipped by design. The explicit auth-gated normal-server check still fails as intended when `PORTAL_BASE_URL=http://127.0.0.1:3001`.
- Attempted `npm run test:portal:workbench`; it failed before the browser segment in the then-current workbench commercial-payload test because `quantityTakeoff.flashings.totalLengthM` was 0 instead of 1.5. The focused commercial payload test failed the same way, so the blocker was tracked separately from the fixture browser gate.

### 2026-05-03

- Expanded commercial parity diagnostics while keeping the path shadow-only: `compareCommercialDesignInputsV1()` now classifies each difference by authored intent, solved geometry, physical takeoff, or commercial mapping, and the focused parity gate passed with 4 files and 33 tests.
- Stabilized the Fixture And QA Gates registry with fixture-only metadata for parity-critical baked workbench fixtures, geometry-ready solved-model assertions, metadata-backed commercial parity checks, representative saved estimate snapshot parity coverage, and supported-fixture takeoff smoke coverage. The then-current fixture/commercial payload/takeoff focused pass covered 3 files and 17 tests. `npm run test:portal:workbench` passed its Vitest segment with 57 files and 583 tests, then the no-auth browser segment was blocked by an existing normal portal Next dev server on port 3001; no pricing behavior changed.
- Added Fixture And QA Gates coverage for the geometry-first workbench path: baked fixture commercial parity dual-produced `calculator_compat` and `workbench_solved` payloads for mono, gable, box, mono-join, and screenshot-style hipped fixtures, and the no-auth browser spec gained a compact plan/3D smoke matrix for the parity-critical fixture shapes. The then-current fixture/commercial payload focused pass covered 9 tests. The full `npm run test:portal:workbench` attempt failed in drawing-surface guard/fallback tests from dirty outside-lane work before it reached the browser segment; direct local browser verification was also blocked by an existing normal portal dev server on port 3001 that auth-gated fixture routes.
- Started the file-decomposition lane with the largest calculator hotspot by extracting pure calculator input/default/normalization helpers from `CalculatorGridClient.tsx` into focused calculator helper modules. `npx vitest run apps/portal/app/staff/calculator` passed with 11 files and 114 tests; the later typecheck restoration pass cleared the unrelated marketing public-token route-test mock drift.
- Continued calculator decomposition by extracting pure save-readiness helpers, estimate-target resolution, redirect builders, and design-request tier labelling into `calculatorSaveWorkflow.ts`. `CalculatorGridClient.tsx` is down to 7065 lines from 7067 at HEAD; `npx vitest run apps/portal/app/staff/calculator` passed with 12 files and 120 tests, and `npm run test:portal:projects` passed with 40 files and 205 tests. A later Workbench drawing-surface pass restored the broad `typecheck` and `test:portal:log` gates.
- Restored the typecheck gate after public-token test contract drift: focused marketing and portal `tsc --noEmit --incremental false` checks passed, `npm run typecheck` passed, marketing public-token route tests passed with 4 files and 21 tests, and focused workbench solved-model/plan-view tests passed with 2 files and 5 tests.
- Restored the Workbench drawing-surface geometry gate after the refactor to the named sheet surface bundle: the import guard now enforces `buildWorkbenchDrawingSurfaceGeometry` and `WorkbenchDrawingSurfaceGeometry`, helper coverage verifies `solved_geometry`, `legacy_fallback`, and `unavailable` sources, `npm run test:portal:workbench` passed with 55 Vitest files and 568 tests plus browser fixture coverage at 3 passed / 1 skipped by design, `npm run typecheck` passed, and `npm run test:portal:log` passed.
- Added `npm run portal:auth-runtime` and wired it into authenticated smoke, performance, and broad `portal:doctor` so staff credentials, role access, schedule readiness, and minimum project data fail before deeper browser assertions.
- Added `npm run portal:side-effects` as the focused quote, invoice, public-token, PDF/email, and job-pack readiness gate. It runs quote/invoice/job-pack focused tests plus the portal build without authenticated browser flows or real email delivery.
- Added `npm run portal:build-env` and wired it into portal build-dependent gates so an active portal dev server or Next build lock fails before `next build`. The preflight is non-destructive and does not stop processes or delete lock files.
- Extended `npm run test:portal:browser` with a no-auth mono fixture assertion for object-first workbench visibility, finite 3D diagnostics, top-projection parity, and absence of user-facing fallback/login/unavailable states.
- Locked projection-backed deck commits to the named `top_projection_to_object_frame` transform, removed stale `commitStartPolygon` bounds remapping, and persisted deck width/depth so rebuilt snapped outlines match the released projection diagnostics.
- Verified the full Design Workbench gate: `npm run test:portal:workbench` passed with 53 Vitest files and 557 tests, then browser fixture coverage passed with 3 no-auth fixture tests and 1 auth-backed smoke skipped by design.
- Added PR-aware architecture changed reporting and strict new-growth advisory reporting to Portal Quality as non-blocking base/head comparisons; strict checks remain visible but unenforced until new-growth enforcement is intentionally enabled.
- Added Knip-backed dead-code reporting with `npm run dead-code:report` and PR-aware `npm run dead-code:changed` advisory output so unused files, exports, types, dependencies, and duplicates can be retired with proof instead of guesswork.
- Confirmed Portal Quality CI enforcement: docs guard, typecheck, lint, portal Vitest, portal build, schedule bundle budget, production security audit, fixture browser smoke, and authenticated smoke are blocking; Portal Performance Report keeps authenticated route timing blocking and now writes auth-runtime prerequisites to the GitHub step summary.
- Attempted authenticated runtime readiness with the supplied temporary account. `portal:auth-env` passed, but Playwright auth setup stayed on `/login` with the app-visible error `Invalid email or password`; authenticated smoke and performance remain Yellow until a valid staff test account exists. Auth setup now clears the password field before throwing so generated text snapshots do not retain the submitted password.
- Verified the full quote/invoice/job-pack side-effects gate after stopping the local portal dev server that held the Next build lock: `npm run portal:side-effects` passed with 8 test files and 32 tests, then `npm run build:portal` completed with `Compiled successfully`, TypeScript, and 55 static pages generated.
- Confirmed the Schedule V2 local readiness gate: `npm run test:portal:schedule` passed with 38 files and 215 tests, including readiness route, Board/Gantt/Site Visits client coverage, Schedule V2 API/RPC command boundaries, and legacy fallback isolation.
- Re-ran `npm run schedule:bundle-budget`; it passed at 589.0 KiB initial raw, 169.1 KiB initial gzip, 333.2 KiB lazy raw, and 78.3 KiB lazy gzip. Live route performance and authenticated smoke remain blocked until staff credentials and a compatible migrated database are available.
- Verified the local security/data-boundary lane: `npm run audit:security`, `npm run browser:supabase`, `npm run service-role:report`, `npm run root:compat`, `npm run architecture:changed`, `npx vitest run apps/portal/lib/supabaseClient.boundaries.test.ts`, `npm run test:portal -- apps/portal/lib/api apps/portal/app/api`, `npm run test:portal:quotes`, `npm run test:portal:schedule`, and `npm run test:marketing` passed. Added marketing public-token route and hash-boundary coverage for missing, invalid, expired, declined/void, accepted, attachment unavailable, and PDF unavailable states.
- Hardened the local-first workflow readiness gate. The focused `npx vitest run apps/portal/lib/localFirst apps/portal/components/sync/LocalFirstPortalMutations.test.tsx apps/portal/lib/estimates apps/portal/app/api/estimates` pass covered 12 files and 67 tests, including new handler assertions for provisional-id retry, durable alias registration, locked estimate/quote conflicts, design-request terminal conflicts, and estimate-notes validation conflicts. The broader `npm run test:portal -- ...` form prepends all of `apps/portal`; it initially exposed an unrelated Design Workbench model-space fixture timing failure fixed in the next note.
- Restored the broad portal Vitest gate by waiting for the workbench snapped-deck selection diagnostic to settle after a failure-feedback canvas click under full-suite load. `npm run test:portal:log` passed with 193 files and 1203 tests; the typecheck-restoration rerun of `npm run test:portal:log` also passed, and `npm run test:portal:workbench` passed with 54 Vitest files, 564 tests, and the no-auth fixture browser smoke at 3 passed / 1 skipped by design.

### 2026-05-02

- Added `npm run portal:fixture-env` and wired it into the no-auth drawing fixture gates so a normal portal dev server or auth-gated `PORTAL_BASE_URL` fails before Playwright startup. The preflight is non-destructive and does not stop existing dev servers.
- Added `npm run portal:auth-env` and wired it into authenticated portal Playwright gates plus broad `portal:doctor` so missing `PORTAL_TEST_EMAIL` or `PORTAL_TEST_PASSWORD` fails before authenticated server startup. The no-auth drawing fixture gate remains independent through `npm run test:portal:browser`.
- Local authenticated smoke and performance verification remain externally blocked until staff test credentials and a compatible portal database are available.
- Replaced quote PDF and job-pack PDF root/app asset probing with module-relative asset URLs. `npm run build:portal` passed with `Compiled successfully`, TypeScript completed, 55 static pages generated, and no Turbopack/NFT trace warnings.
- Removed stale compatibility-client mocks from contacts/projects/project snapshot tests by routing contact writes and project snapshots through auth-bound staff Supabase clients. `npm run test:portal:projects` passed with 40 files and 197 tests; `npm run portal:doctor:quick` passed.

### 2026-05-01

- Restored the no-auth drawing fixture browser gate: `npm run test:portal:browser` passed locally with 2 fixture tests passing and the auth-backed project discovery smoke skipped by design. The remaining browser-gate work is authenticated smoke and performance smoke with staff test credentials.
- Added the portal speed tooling command plan: focused portal test scripts, `portal:doctor:quick`, `portal:doctor`, and matching CI/doc routing.
- Added logged gate variants for agent-friendly verification: `portal:doctor:quick:log`, `portal:doctor:log`, and `test:portal:log` run the same source-of-truth commands while keeping noisy stdout/stderr in OS temp logs.
- Easy health pass updated current gate status: typecheck, lint, docs guards, portal tests, portal build, schedule bundle budget, and production audit are green locally; portal build remains Yellow because of the Turbopack/NFT quote PDF tracing warning; fixture browser smoke now fails loudly when fixture coverage cannot run instead of silently skipping.
- Verification note: after the fixture smoke skip policy changed, `npm run test:portal:browser` fails locally with "Fixture workbench route requires staff auth in this portal environment; browser smoke coverage did not run" instead of reporting a false-green skipped suite.
- Cleared the quick-doctor typecheck blocker by typing the semantic placement cases in the then-current workbench model-space fixture suite; `npm run typecheck` passed.
- `npm run portal:doctor:quick` passed when captured to a log: docs guard, mojibake, typecheck, lint, and `npm run test:portal` all completed; portal tests reported 172 files and 1077 tests passed.
- `npm audit --omit=dev` reported 0 vulnerabilities. Full local authenticated smoke still needs `PORTAL_TEST_EMAIL` and `PORTAL_TEST_PASSWORD`; fixture browser smoke can run without auth.
- Created this tracker to coordinate portal production-readiness work.
- Initial review identified quality gates as the highest leverage priority before broad feature expansion.
- Known review findings to re-verify: portal tests failing, lint guard failing, schedule bundle budget failing, and production audit reporting vulnerabilities.
- Parallel lanes identified: quality gate repair, security/deps, contacts/projects env boundaries, schedule performance, design workbench behavior, quote/invoice/job-pack side effects, style isolation, CI/typecheck/tooling, and large-file decomposition after gates are green.
