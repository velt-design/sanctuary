import AnimatedRouteTemplate from '@/components/AnimatedRouteTemplate';

export default function Template({ children }: { children: React.ReactNode }) {
  return <AnimatedRouteTemplate>{children}</AnimatedRouteTemplate>;
}
